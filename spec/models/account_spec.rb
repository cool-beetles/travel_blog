require 'rails_helper'

describe Account, type: :model do
  describe "validations" do

    context "when e-mail and password are correct" do
      it "is valid" do
      end
    end

    context "when e-mail is incorect" do
      it "is invalid" do
      end
    end

    context "when passowrd is incorect" do
      it "is invalid" do
      end
    end

  end
end